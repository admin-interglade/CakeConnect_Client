/**
 * Authentication — FR-1, FR-2.
 *
 * Endpoints: `/auth/send-otp`, `/auth/verify-otp`, `/auth/login`,
 * `/auth/refresh-token`, `/auth/logout`, `/auth/me`, `/users/profile`.
 */
export * from './auth.api';
